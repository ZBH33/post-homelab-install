# Enforce consistent line endings and text handling
* text=auto

# Shell scripts should use LF
*.sh eol=lf

# PowerShell on Windows uses CRLF; keep CRLF for PS files
*.ps1 eol=crlf

# YAML/MD files text
*.yml text=auto
*.yaml text=auto
*.md text=auto

# Treat vendored directories as vendored for linguist (if applicable)
/vendor/** linguist-vendored

# Export rules for source archives (optional)
# Exclude local dev artifacts from source exports
dist/** export-ignore
build/** export-ignore

# Keep repository placeholder env file tracked (document-only)
# If you'd prefer it excluded from source archives, uncomment below:
